# Server
PORT=4000
NODE_ENV=development

# JWT
JWT_SECRET=opsportal_super_secret_jwt_key_2026_change_in_production
JWT_EXPIRES_IN=7d

# PostgreSQL Database (same Supabase Postgres instance)
DB_HOST=db.your-project.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your-db-password
